{
  "name": "contake",
  "version": "0.1.0",
  "private": true,
  "description": "Contake Alpha monorepo: one generic core + domain profiles.",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "dev:api": "pnpm --filter @contake/api dev",
    "seed:api": "pnpm --filter @contake/api seed"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": { "node": ">=22" }
}
